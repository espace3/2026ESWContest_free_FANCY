"""
target_fan / config.py
전체 프로젝트 설정을 한 곳에서 관리합니다.

실기 검증을 마친 값만 둡니다. 미결 항목은 여기 적지 않고
docs/hardware_todo.md 에 모읍니다.
"""

from pathlib import Path

# 캡처 해상도.
_CAM_W, _CAM_H = 640, 360

# ── 화각 (Pi Camera Module 3 Wide NoIR / IMX708) ─────────────────────────────
# 하드웨어·캡처 비율 고정 (2026-08-27): Wide NoIR 단일 기종, 16:9 캡처.
# 데이터시트 공칭 화각(수평 102° / 수직 67°).
_FOV_H_DEG = 102.0
_FOV_V_DEG = 67.0

# ── BLE 프로토콜 (docs/ble_protocol.md · 앱과 반드시 일치) ───────────────────
# 튜닝값이 아니라 앱팀과 합의한 계약이다 — 바꾸면 앱도 같이 바꿔야 한다.
# 예전에는 app/ble_protocol.py 안에 있어 앱쪽에서 찾기 어려웠다 (2026-09-02 이동).
UUID_BASE = "14d7{:04x}-7197-49e5-a017-0b2f308120f0"
SERVICE_UUID = UUID_BASE.format(0x0001)
POWER_UUID = UUID_BASE.format(0x0002)
MODE_UUID = UUID_BASE.format(0x0003)
WIND_UUID = UUID_BASE.format(0x0004)
STATUS_UUID = UUID_BASE.format(0x0005)

LOCAL_NAME = "ESW-FAN"

MODE_NAMES = {0x00: "기본-고정", 0x01: "기본-회전", 0x02: "타겟", 0x03: "타겟-부위"}
WIND_TARGETS = {0x00: "공용", 0x01: "머리", 0x02: "상체", 0x03: "하체"}
TARGET_MODES = (0x02, 0x03)   # 카메라 추적을 쓰는 모드


CFG: dict = {
    # ── 카메라 ──────────────────────────────────────────────────────────────
    "camera": {
        "width":  _CAM_W,
        "height": _CAM_H,
        # 캡처 fps는 곧 추론 FPS의 상한이다. rpicam-vid 백엔드에 그대로 전달되고
        # (OpenCV는 드라이버가 무시할 수 있음), Picamera2 백엔드는 쓰지 않는다.
        "fps":    30,
    },

    # ── 화각 (degree) — 위 _FOV_*_DEG 참고 ──────────────────────────────────
    "fov": {
        "h": _FOV_H_DEG,
        "v": _FOV_V_DEG,
    },

    # ── GPIO 핀 (BCM 번호) ──────────────────────────────────────────────────
    # 모터 단독 실기 검증을 마친 배선 (아두이노 CNC v3 쉴드 + TMC2209).
    # EN은 쉴드 구조상 전 축이 단일 핀(24)을 공유 — 모터 개별 disable 불가.
    # 운용 중에는 켜둔 채 STEP/DIR로만 축별 제어한다.
    "pins": {
        "pan":  {"EN": 24, "STEP": 23, "DIR": 22},   # X축: 유성기어
        "tilt": {"EN": 24, "STEP": 20, "DIR": 21},   # Y축: 웜기어
        # 릴레이 IN(레벨컨버터 LV측), 물리 핀 33/35/37. IN4는 완전 미사용.
        "relay": {"IN1": 13, "IN2": 19, "IN3": 26},  # 미풍/약풍/강풍
    },

    # ── 릴레이 (TS0011 4ch — 선풍기 풍속 220V 탭 단속) ──────────────────────
    # 옵토 미사용. IN을 LOW로 당기면 코일 여자 → NO-COM 닫힘 = 탭 ON (active-LOW).
    # 동시에 하나의 채널만 닫는다 (전부 오픈 = 정지). hardware/relay_control.py 참고.
    "relay": {
        # 2026-08-04 실측 확정: active-LOW 기판(LOW=ON).
        "active_high": False,
        "guard_s": 0.1,        # 전환 시 전부 오픈 후 대기(초) — 역전류/접점 아크/모터 back-EMF 방지. 실측 튜닝 잔여.
        # BLE 풍속 세기(1~3) → 릴레이 채널. 0/범위 밖 = 정지(전부 오픈).
        "level_pins": {1: "IN1", 2: "IN2", 3: "IN3"},
    },

    # ── 스테퍼 구동 파라미터 (모터 단독 검증 + 실측 캘리브레이션) ────────────
    # 스텝당 각도 = 360 / (steps_per_rev × microstep × gear_ratio)
    #   pan ≈ 0.001125° (1:100 유성), tilt ≈ 0.001215°
    "stepper": {
        "steps_per_rev": 200,  # 모터 라벨 1.8°/STEP 확인 (2026-07-08)
        "microstep": 16,       # 실측
        # dir_for_positive: 각도가 +방향으로 늘 때 DIR 핀에 쓸 값 (0|1).
        # 두 축 모두 실기에서 "화면 오른쪽/아래 = +각도"로 확인 완료.
        "pan": {
            "gear_ratio": 100,     # 유성 기어 라벨값
            "dir_for_positive": 0,
            "f_start": 800,
            "f_max": 7000,
            "ramp_segments": 12,
            "ramp_steps_per_seg": 60,
        },
        "tilt": {
            "gear_ratio": 92.6,    # 무부하 벤치 실측 (라벨은 100)
            "dir_for_positive": 0,
            "f_start": 800,
            "f_max": 6000,
            "ramp_segments": 12,
            "ramp_steps_per_seg": 60,
        },
    },

    # ── 모터 위치 상태 파일 (hardware/position_store.py) ────────────────────
    # 스테퍼는 오픈루프라 절대 위치를 잴 수단이 없다. 리밋 스위치/엔코더를 달기
    # 전까지의 방편으로, 장부(보낸 펄스 수)를 이 파일에 남겨 재시작 때 되살린다.
    # 헤드가 하나이므로 모든 스크립트가 이 파일 하나를 공유한다 — 스크립트마다
    # 다른 경로를 쓰면 A로 돌려놓고 B로 실행했을 때 B가 낡은 값으로 엉뚱하게
    # 움직인다. 한계(탈조·마지막 저장 이후 유실)는 position_store.py docstring 참고.
    "motor_state": {
        # 레포 루트 기준 절대경로 — 상대경로로 두면 실행 디렉터리가 다를 때
        # 엉뚱한 위치에 새 파일이 생겨 복원이 어긋난다.
        "file": str(Path(__file__).resolve().parent / "motor_state.json"),
    },

    # ── 회전 소프트 리밋 (°) ─────────────────────────────────────────────────
    # 회전 금지 구역(HW deadline)은 조립 후 확정 (docs/hardware_todo.md).
    # control 쪽 clamp_angle()에 물려서 쓴다 (motor_control는 재검사하지 않음).
    #
    # ⚠ 틸트 부호: +가 아래(헤드가 숙임), -가 위다
    #   (control_signal.compute_tilt_angle — 화면 아래일수록 양수).
    # ⚠ max(+, 아래)는 **+15를 절대 넘기지 말 것** — 헤드가 15도보다 더 숙이면
    #   기구가 간섭해 파손된다 (2026-08-28 사용자 확인, 실측 한계).
    #   min(-, 위)은 여유가 있어 -25까지 확장했다 (같은 날 확인).
    # 참고: 헤드(카메라 렌즈) 높이 92cm 실측. 이 리밋에서 거리 D일 때
    #   도달 높이 = 92 + D·tan25 (위) ~ 92 - D·tan15 (아래).
    #   1.5m에서 162~52cm로 머리·가슴·상체가 모두 들어온다. 무릎(48cm)은
    #   2m 이상에서만 닿는다 — 아래 리밋이 파손 한계라 더 못 내린다.
    "limits": {
        # 팬은 ±100 이면 충분하다 — 회전 모드 스윕이 ±60 이고, 추적은 사람이
        # 화면에 있어야 성립한다. 예전에는 추적 루프만 ±100(--limit), 나머지는
        # ±200 으로 갈려 있었다 (2026-09-02 통일).
        "pan":  {"min": -100.0, "max": 100.0},
        "tilt": {"min": -25.0,  "max": 15.0},
    },

    # ── 추적 루프 튜닝값 ─────────────────────────────────────────────────────
    # 두 진입점(main.py / app/camera.py)의 CLI 기본값이 여기서 나온다. 예전에는
    # 같은 숫자가 진입점마다 각각 박혀 있어 한 곳만 고치면 다르게 도는데도 경고가
    # 없었다(실제로 gain.tilt가 0.2와 0.25로 갈라져 있었다 — 2026-09-02에 0.2로 통일).
    # CLI 인자로 덮어쓰는 것은 그대로 된다.
    #
    # 값의 근거는 실기 관측이다(모델·데이터시트 권장값이 있는 항목이 아니다).
    # 틸트가 팬보다 작은 이유: 가동 범위가 40°(-25~+15) 대 400°로 열 배 좁아
    # 같은 각도 오차가 훨씬 크게 작용하고, 실측 순항 속도도 느리다.
    "tracking": {
        # 비례 게인 — 오차각의 몇 배를 목표에 반영할지. 크면 빠르고 진동하기 쉽다.
        "gain":     {"pan": 0.3, "tilt": 0.2},
        # 데드존(°) — 직전에 보낸 명령과 이만큼 이하로 차이 나면 안 보낸다.
        "deadzone": {"pan": 1.0, "tilt": 0.5},
        # 조준점(정규화 화면 좌표) — 0.5, 0.5가 화면 정중앙.
        "target":   {"cx": 0.5, "cy": 0.57},
        # 두 문턱은 층이 다르다 — person_score 로 사람인지 먼저 거르고,
        # 통과한 사람 안에서 conf 로 관절 하나하나를 거른다.
        "conf": 0.25,              # 키포인트(관절) 신뢰도 문턱
        "min_person_score": 0.15,  # 인물 검출 점수 문턱
        "threads": 3,      # TFLite 워커 스레드 수 (Pi 5 기준, FPS 튜닝 시 조정)
    },
}
