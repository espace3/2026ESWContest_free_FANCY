"""
target_fan / config.py
전체 프로젝트 설정을 한 곳에서 관리합니다.

2026-07 정리: 구세대 설계(Pi 4B + 서보모터 + MediaPipe + WebSocket 서버 + IR 리모컨 +
LLM 명령 해석) 항목을 전부 제거하고, 현행 설계(Pi 5 + 스테퍼 모터 + MoveNet MultiPose +
BLE)에서 실제로 쓰이거나 확정된 값만 남겼습니다. 과거 값이 필요하면 git 히스토리를
보세요. 스테퍼 핀·구동 파라미터는 scripts/verify_motor.py 실기 검증값을 반영했고
(2026-07-08), 릴레이 핀·파라미터는 2026-08-04 배선 확정(물리버튼 제거, TS0011
직접 탭 단속)을 반영했습니다 — 실측 잔여 항목은 hardware/TODO.md 참고.
"""

import math
from pathlib import Path

# 캡처 해상도 — 아래 fov/focal_px가 이 값 기준으로 유도되므로 여기서만 수정할 것.
# 16:9(640x360)로 캡처해 센서 전체 화각을 크롭 없이 그대로 쓴다 (아래 화각 설명 참고).
_CAM_W, _CAM_H = 640, 360

# ── Pi Camera Module 3 (IMX708) 화각 ─────────────────────────────────────────
# 데이터시트 공칭 화각(표준형 수평 66° / 수직 41°)은 센서 전체(16:9) 출력 기준이다.
# 그래서 16:9 비율(640x360 등)로 캡처하면 크롭이 없어 이 공칭값을 그대로 쓴다.
#
# [과거 값과 그 이유] 이전에는 640x480(4:3)으로 캡처하고 수평 52° / 수직 41°를
# 썼다. 4:3이 16:9보다 정사각형에 가까워서, MoveNet 입력(160x160 정사각형)으로
# 리사이즈할 때 가로 찌그러짐이 덜하다는 이점 때문이었다. 대신 16:9 센서에서
# 4:3을 뽑으려면 가로를 잘라(crop) 써서 유효 수평 화각이
#     2 * atan( (3/4) * tan(66°/2) ) ≈ 52°
# 로 줄었다 (세로 41°는 유지, Wide는 같은 공식으로 102°→85.6°였다). 지금은 전체
# 화각을 쓰려고 16:9로 바꿔 크롭값(52/85.6) 대신 공칭값(66/102)을 그대로 쓴다.
#   - 다시 4:3으로 돌아가면 h를 위 크롭 공식으로 재계산할 것
#   - NoIR은 광학계가 컬러형과 동일해서 이 값 그대로 유효
# 모터 위치 정확도 목표(<2°)에 직결되므로 최종적으로는 실측 검증 권장:
# 카메라 정면 거리 D(m)에서 옆으로 X(m) 이동했을 때 compute_pan_angle() 결과가
# atan(X/D)와 일치하는지 확인하면 된다.
_FOV_H_DEG = 66.0
_FOV_V_DEG = 41.0

# Camera Module 3 Wide 렌즈(공칭 수평 102° / 수직 67°)로 교체 시 화각. 16:9 캡처라
# 크롭 없이 공칭값 그대로다 (4:3이었을 땐 85.6°/67°였다 — 위 설명 참고). IMX708
# 센서 칩은 표준/Wide/NoIR 모듈이 다 동일해 소프트웨어가 렌즈를 자동 구분할 수
# 없으므로, 추적 스크립트의 --wide 플래그로 명시할 때만 이 값을 쓴다.
_FOV_WIDE_H_DEG = 102.0
_FOV_WIDE_V_DEG = 67.0

CFG: dict = {
    # ── 카메라 ──────────────────────────────────────────────────────────────
    "camera": {
        "width":  _CAM_W,
        "height": _CAM_H,
        # 카메라 프레임레이트는 곧 추론 FPS의 상한이다 — 구세대 값 20(Pi 4B CPU
        # 절약용)을 그대로 두면 추론이 20fps를 넘는지 확인할 수 없어 30으로 올림.
        "fps":    30,
    },

    # ── 카메라 FOV (화각, degree) ────────────────────────────────────────────
    "fov": {
        "h": _FOV_H_DEG,
        "v": _FOV_V_DEG,
    },

    # Wide 렌즈용 화각 (--wide 플래그로 선택). 위 fov와 같은 구조.
    "fov_wide": {
        "h": _FOV_WIDE_H_DEG,
        "v": _FOV_WIDE_V_DEG,
    },

    # ── GPIO 핀 (BCM 번호) ──────────────────────────────────────────────────
    # scripts/verify_motor.py로 실기 검증된 배선 (아두이노 CNC v3 쉴드 + TMC2209).
    # EN은 쉴드 구조상 전 축이 단일 핀(24)을 공유 — 모터 개별 disable 불가.
    # 운용 중에는 켜둔 채 STEP/DIR로만 축별 제어한다 (enable 상태에서는 정지
    # 중에도 유지 전류가 흘러 발열하므로, 장시간 유휴 시 전체 disable 정책은
    # 추후 결정 — hardware/TODO.md).
    "pins": {
        "pan":  {"EN": 24, "STEP": 27, "DIR": 22},   # X축: 유성기어 (유효비 확인 필요)
        "tilt": {"EN": 24, "STEP": 20, "DIR": 21},   # Y축: 웜기어 (유효비 92.6 실측)
        # 릴레이 IN(레벨컨버터 LV측), 물리 핀 33/35/37. IN4는 완전 미사용.
        # ⚠ 이 핀들을 GPIO9~27(부팅 시 pull-DOWN=LOW) 대역에서 고른 원래 근거는
        # "LOW=오픈"이었으나, 2026-08-04 실측으로 기판이 active-LOW로 확인되어
        # (LOW=ON) 그 근거는 무효다. 지금 부팅 중 안전은 레벨컨버터 HV측 풀업이
        # IN을 5V(=OFF)로 띄워주는지에 전적으로 의존한다 — 실측 필수
        # (hardware/TODO.md 부팅 안전 항목).
        "relay": {"IN1": 13, "IN2": 19, "IN3": 26},  # 미풍/약풍/강풍
    },

    # ── 릴레이 (TS0011 4ch — 선풍기 풍속 220V 탭 단속) ──────────────────────
    # 옵토 미사용. IN을 LOW로 당기면 코일 여자 → NO-COM 닫힘 = 탭 ON (active-LOW).
    # 동시에 하나의 채널만 닫는다 (전부 오픈 = 정지). hardware/relay_controller.py 참고.
    "relay": {
        # 2026-08-04 실측 확정: active-LOW 기판(LOW=ON). 처음엔 True로 가정했으나
        # verify_relay.py에서 "정지 명령에 세 채널 전부 닫힘 / 미풍 명령에 나머지
        # 두 채널만 닫힘"이 관측되어 반전 확인. 따라서 안전 상태(전부 오픈)는 HIGH다.
        "active_high": False,
        "guard_s": 0.1,        # 전환 시 전부 오픈 후 대기(초) — 역전류/접점 아크/모터 back-EMF 방지. 실측 튜닝 잔여.
        # BLE 풍속 세기(1~3) → 릴레이 채널. 0/범위 밖 = 정지(전부 오픈).
        "level_pins": {1: "IN1", 2: "IN2", 3: "IN3"},
    },

    # ── 스테퍼 구동 파라미터 (verify_motor.py + 실측 캘리브레이션) ────────────
    # 스텝당 각도 = 360 / (steps_per_rev × microstep × gear_ratio)
    #   pan ≈ 0.001125° (1:100 유성), tilt ≈ 0.001215°
    "stepper": {
        "steps_per_rev": 200,  # 모터 라벨 1.8°/STEP 확인 (2026-07-08)
        "microstep": 16,       # 실측
        # dir_for_positive: 각도가 +방향으로 늘 때 DIR 핀에 쓸 값 (0|1).
        # 임시값 — 실기에서 "화면 오른쪽/아래 = +각도" 방향과 일치하는지 확인 후
        # 필요하면 뒤집을 것 (hardware/TODO.md).
        "pan": {
            "gear_ratio": 100,     # 유성 기어 모터 (아래 주석 값은 1 기준)
            "dir_for_positive": 0, # 확인 완료
            "f_start": 800,        # 700
            "f_max": 6000,         # 2000
            "ramp_segments": 12,
            "ramp_steps_per_seg": 60, # 90
        },
        "tilt": {
            # 92.6 = 실측 (라벨은 100). 무부하 벤치 측정값 — 부하(팬 헤드)
            # 장착 후 재확인할 것.
            "gear_ratio": 92.6,
            "dir_for_positive": 0, # 정면 설정 완료 (2026-07-10)
            "f_start": 800,
            # ⚠ 임시 실험값 (2026-08-26) — 원래 6000. 팬+틸트 동시 구동 시 팬
            # 드라이버가 출력 래치되는 현상이 회생 스파이크 때문인지 가르려고
            # 절반으로 낮췄다 (회생 에너지 ∝ 속도²이므로 1/4로 준다). 판정이
            # 끝나면 6000으로 되돌릴 것 — 틸트는 이 값에서도 이미 속도가
            # 부족하다 (hardware/TODO.md "틸트 최고 속도 부족").
            "f_max": 3000,
            "ramp_segments": 12,
            "ramp_steps_per_seg": 60,
        },
    },

    # ── 모터 위치 상태 파일 (hardware/position_store.py) ────────────────────
    # 스테퍼는 오픈루프라 절대 위치를 잴 수단이 없다. 리밋 스위치/엔코더를 달기
    # 전까지의 방편으로, 장부(보낸 펄스 수)를 이 파일에 남겨 재시작 때 되살린다.
    # 헤드가 하나이므로 모든 스크립트가 이 파일 하나를 공유한다 — 스크립트마다
    # 다른 경로를 쓰면 A로 돌려놓고 B로 실행했을 때 B가 낡은 값으로 엉뚱하게
    # 움직인다. 한계(탈조·전원 차단 중 외력)는 position_store.py docstring 참고.
    "motor_state": {
        # 레포 루트 기준 절대경로 — 상대경로로 두면 실행 디렉터리가 다를 때
        # 엉뚱한 위치에 새 파일이 생겨 복원이 어긋난다.
        "file": str(Path(__file__).resolve().parent / "motor_state.json"),
    },

    # ── 회전 소프트 리밋 (°) ─────────────────────────────────────────────────
    # 회전 금지 구역(HW deadline)은 조립 후 확정 (hardware/TODO.md). 확정 전까지의
    # 보수적 임시값 — 특히 tilt는 축 방향·기구 한계 미실측이라 좁게 잡았다.
    # control 쪽 clamp_angle()에 물려서 쓴다 (motor_controller는 재검사하지 않음).
    "limits": {
        "pan":  {"min": -200.0, "max": 200.0},
        "tilt": {"min": -15.0,  "max": 15.0},
    },

    # ── 거리 추정 (어깨 너비 기반 — control/control_signal_generator.py) ─────
    "distance": {
        "ref_shoulder_cm": 40,   # 성인 평균 어깨 너비 (cm)
        # 초점거리(픽셀) — 수평 FOV에서 유도. 어깨 너비는 화면상 수평 거리라서
        # 수평 기준을 쓴다. 해상도/FOV를 바꾸면 자동으로 따라온다.
        "focal_px": (_CAM_W / 2) / math.tan(math.radians(_FOV_H_DEG / 2)),  # ≈ 493
        # estimate_distance_m(min_m=…, max_m=…)에 넘겨서 키포인트 잡음으로 인한
        # 비정상 추정값(초근거리/초원거리)을 잘라내는 용도
        "min_m": 0.3,
        "max_m": 5.0,
    },

    # ── 팬틸트 모터 회전 속도 거리 구간 (m) ──────────────────────────────────
    # 주의: 선풍기 풍속(약풍/중풍/강풍) 아님! 추정 거리로 팬틸트가 목표를 따라가는
    # "회전 속도" 단계를 고르는 용도다 (motor_speed_zone() 참고). 풍속은 사용자가
    # 앱에서 BLE로 부위별로 직접 지정하는 값이라 거리 추정과는 무관하다.
    "speed_zones": {
        "near": 1.0,   # ~1m
        "mid":  2.0,   # ~2m (초과하면 3단계)
    },
}
