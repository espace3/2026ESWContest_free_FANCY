# 모터가 시끄럽거나 멈출 때 — 진단 기록

Pi 5에서 스테퍼를 돌리며 만난 세 가지 문제와 그 해결 과정. 증상이 비슷해 보여도
원인이 전혀 달라서, **어느 지문이 어느 원인을 가리키는지**를 남겨 둔다.

| # | 증상 | 원인 | 대책 | 해결 |
|---|---|---|---|---|
| 1 | 부하 시 **달그락 소음**, 드리프트 3~7% | 펄스 스레드가 SCHED_OTHER라 선점당함 | `stepper.open_chip()` RT 승격 | 2026-08-07 |
| 2 | 두 축이 **함께** 멎고 재시작 전엔 안 돌아옴 | liblgpio `clock_nanosleep` EINVAL 무한 재시도 | `tools/patch_lgpio.sh` | 2026-08-26 |
| 3 | 두 축 동시 구동 시 **팬만** 멎음 | 드라이버 VREF 과다 → 과전류 보호 | 양축 0.7V | 2026-09-02 |

아래 본문은 **1번(지터)** 의 상세다. 2·3번은 문서 끝에 있다.

> ⚠ 이 파일은 원래 2026-07-16에 작성됐다가 유실됐고(레포에 없는 채로 하드웨어 TODO에서만
> 참조되고 있었다), 2026-08-07에 재작성됐다. 당시 "적용했다"던 RT 패치는 절차도
> 유실됐고 실행 환경(pyenv venv)에서 무효였다. **그 전철을 밟지 않으려고 이번 대책은
> 문서가 아니라 코드 안에 넣었다** — venv를 새로 만들어도, 이 문서를 못 찾아도 따라온다.
> 적용 여부는 아래 "적용 확인법"으로 언제든 다시 확인할 수 있다.

---

## 왜 이 문제가 구조적으로 존재하는가

Pi 5는 pigpio(DMA 기반 하드웨어 타이밍)를 쓸 수 없어 lgpio만 선택지인데, lgpio의
`tx_pwm`은 **소프트웨어 타이밍**이다. 즉 펄스 간격을 커널 스케줄러에 의존하는 스레드가
만들어낸다. 그 스레드가 선점당하면 스텝 펄스 간격이 불규칙해지고, 로터가 그 지터를
따라 덜컹거린다. 심하면 스텝을 잃는다(탈조).

추적 중에는 한 프로세스에 스레드가 22개(TFLite 3 + 메인 루프 + 카메라 리더 + lgpio +
…)인데 코어는 4개다. 초과 구독 상태라 펄스 스레드가 쉽게 밀린다.

## 이력

- **2026-07-16** — 부하 시 "펄스 몰림 → 탈조" 관측. 대응으로 tilt `f_max`를
  10000 → 6000으로 잠정 하향 (config.py `stepper.tilt.f_max`). 원인 문서(이 파일)와
  패치 절차를 작성했으나 유실.
- **2026-07-24** — "lgpio RT 우선순위 패치 적용됨(문서 절차 3)"이라고 하드웨어 TODO에 기록.
  **아래 2026-08-07 확인에 따르면 현재 실행 환경에서는 무효다.**
- **2026-08-07** — 추적 중 달그락 소음의 원인을 CPU 경쟁으로 확정 (아래).
- **2026-08-26** — 2번(EINVAL 스핀) 확정. 아래 RT 승격이 만든 `PR=-11` 지문 덕에
  범인을 특정할 수 있었다.
- **2026-09-02** — 3번(VREF) 해결.

## 2026-08-07 진단

### 증상

카메라 추적(`main.py`)을 돌리면 모터에서
달그락거리는 소리가 난다. `tools/drive_motor.py` 단독 구동은 어떤 조건(긴 이동/왕복/잔이동/
넓은 범위)에서도 조용하다. 소리는 **모터가 실제로 도는 동안에만** 난다.

### 원인

lgpio의 펄스 생성 스레드가 일반 우선순위(SCHED_OTHER)로 돌아서, CPU가 초과 구독되면
선점당해 스텝 펄스 간격이 흔들린다.

### 결정적 실험 (이 순서로 재현 가능)

| 조건 | 결과 |
|---|---|
| busy loop 3개(코어 1개 여유) + `drive_motor --sweep 60 --cycles 3` | 조용 |
| busy loop 6개(코어 초과 구독) + 같은 명령 | **소리** |
| busy loop 6개 + `sudo chrt -f 10 $(which python3) …`로 같은 명령 | **조용** ← 확정 |
| 추적 중 `--threads 3 → 1` | 소리 줄어듦 |

```bash
# 부하 만들기 / 없애기
for i in 1 2 3 4 5 6; do (while :; do :; done) & done
kill %1 %2 %3 %4 %5 %6
```

### 실행 환경 상태 (실기 확인)

```
$ ps -eLo pid,tid,cls,rtprio,comm | grep -i python
   7311    7311  TS      - python3        ← 22개 스레드 전부 cls=TS, rtprio=-
   ...                                       RT 우선순위가 아무 데도 없다

$ python3 -c "import lgpio; print(lgpio.__file__)"
/home/esw/.pyenv/versions/fancy/lib/python3.11/site-packages/lgpio.py
```

실행이 pyenv 가상환경(`fancy`)에서 이뤄지는데, 거기 pip로 설치된 lgpio가 시스템에
설치했을 패치본을 가리고 있다. **2026-07-24의 "패치 적용됨" 기록은 이 환경에서 무효다.**

### 이미 배제된 가설 — 같은 길을 다시 걷지 말 것

| 가설 | 어떻게 검증했나 | 결과 |
|---|---|---|
| 데드존/게인이 만드는 정지→기동 반복 | `--deadzone-pan 0.3 --gain-pan 0.2` | 무관 (그대로) |
| 큐 언더런 (`_LOOKAHEAD_S` 부족) | 0.09 → 0.25 → 1.0 | **무관.** 1초 버퍼도 안 통함 = 큐가 비는 게 아니라 개별 펄스 간격이 흔들리는 것. 버퍼로는 못 고친다 |
| 두 축 동시 구동 (tx_pwm 2핀 경합) | 단일 축 추적에서도 발생 | 무관 |
| Camera Module 3 오토포커스(VCM) 소음 | 소리 발생원이 모터임을 직접 확인 | 무관 |
| 팬 기어비 오차 | 360° 명령에 오차 3°, 1080°에서도 안 커짐 | 무관 (회전량에 비례하지 않음 = 백래시) |
| 이동 패턴 (잔이동 vs 긴 이동) | `--track-sim`, `--sweep` 모두 무부하에서 조용 | 무관 |
| 발열 | 미측정 | 주 원인은 아님 (부하만 걸면 즉시 재현) |
| **커널 타이머 슬랙** (비-RT 스레드 nanosleep 기본 50us) | 드리프트 1%면 에지당 0.83us — 슬랙을 먹는다면 60%대로 나와야 한다 | **무관.** lgpio는 절대시간 sleep을 쓰거나 스핀한다. `prctl(PR_SET_TIMERSLACK)`은 효과 없음 |
| 위치 저장 스레드의 SD fsync | 상태 파일을 `/dev/shm`(tmpfs)로 돌려 SD I/O 제거 | 무관 (스파이크 그대로) |

## 재는 법 (대책을 고르기 전에 숫자부터)

귀로 "조용/시끄러움"만 비교하면 대책 다섯 개 중 뭐가 들었는지 못 가른다. 두 가지를
만들어뒀다.

**① 실측 — `--timing` (권장).** 진짜 시스템에서 직접 잰다. 배선 불필요.
`_sched_end`가 이미 "큐에 넣은 펄스가 다 나갈 예정 시각"을 들고 있으므로 실제 완료
시각과 비교하면 된다 (`stepper._Axis._execute`).

    계획   = Σ(청크 스텝수 / 주파수)      실측 = 첫 청크 큐잉 ~ 마지막 펄스 송출 완료
    드리프트 = 실측 - 계획 > 0  →  펄스 열이 늘어졌다 = 펄스 스레드가 선점당했다
    언더런  = 앞 청크가 다 나간 뒤에야 다음 청크를 넣은 횟수 (= 펄스가 실제로 끊김)

```bash
python tools/drive_motor.py --sweep 60 --cycles 3 --timing   # 부하 없는 기준선
python main.py --axis pantilt --timing     # 소리 나는 그 조건
python main.py --axis pantilt --timing --no-rt --no-pin  # 대책 끄고 비교
```

**② 대리 측정 — `tools/measure_jitter.py`.** 모터·배선 없이 "스레드가 예정보다
얼마나 늦게 깨는가"만 잰다. 대책 후보를 코드에 넣기 전에 A/B 하는 용도.

## 실측값 (2026-08-07, Pi 5 4코어, f_max 6000 → 에지 간격 T=83.3us)

`tools/measure_jitter.py`, 부하는 busy loop 6개. "초과분"은 바닥(sleep 오버헤드)을 뺀 값:

| 조건 | 바닥 | p90 | p99 | max | T 초과 |
|---|---|---|---|---|---|
| 무부하 | 52.7us | 2.4 | 3.2 | 442.9 | 0.01% |
| 부하 6 | 54.2us | 17.9 | 33.8 | 522.7 | 0.04% |
| `chrt -f 10` + 부하 6 | 5.8us | 10.9 | 16.3 | **46.2** | **0%** |
| 슬랙 1ns + 부하 6 | 4.3us | 17.3 | 32.4 | 672.3 | 0.03% |

→ **슬랙은 바닥만 낮추고 꼬리를 못 건드린다. RT는 둘 다 잡는다.** max 522→46us가
결정적이다.

`--timing` 드리프트(실모터, 추적 중):

| 상태 | 드리프트 |
|---|---|
| 대책 없음 | 3~7% |
| 어피니티만 | ~1%, 가끔 3% |
| 어피니티 + RT | 그보다 낮음, 소리 거의 사라짐 |

언더런은 어느 조건에서도 0이었다 — **큐가 비는 게 아니라 펄스 스레드 자체가 느려진
것**이라는 최종 확인이다.

## 적용된 대책 (`stepper.open_chip()`)

스레드는 **만든 쪽의 어피니티 마스크와 스케줄링 정책을 물려받는다** (glibc의
`pthread_create` 기본값이 `PTHREAD_INHERIT_SCHED`). 이 상속을 쓰면 tid를 알아낼 필요도,
`/proc/self/task` 차집합으로 스레드를 특정할 필요도 없다:

```
① 호출 스레드 → 펄스 코어 하나(마지막 코어) + SCHED_FIFO(prio 10)
② gpiochip_open()            ← lgpio 펄스 스레드가 둘 다 상속
③ 호출 스레드 → SCHED_OTHER + 나머지 코어(계산용)로 복구   ★ 필수
④ 이미 떠 있던 스레드(TFLite 등)도 계산 코어로 몰아냄
```

- **③이 필수인 이유**: 복구를 빠뜨리면 그 뒤에 만들어지는 워커·저장 스레드까지 RT가
  된다. 저장 스레드는 fsync를 하므로 RT로 돌면 위험하다. `finally`로 무조건 복구한다.
- **④가 필요한 이유**: 스크립트들이 모터보다 detector를 먼저 만든다. 사후에 옮겨주면
  스크립트 순서를 안 고쳐도 된다.
- RT는 `rtprio` 권한이 필요하다. 없으면 경고만 찍고 어피니티만 적용한다(죽지 않는다).
- 끄는 법: `--no-pin` / `--no-rt` (효과 비교용). 판정은 `--timing` 드리프트로.

### 적용 확인법

시작할 때 찍히는 줄:

```
[motor] 펄스 스레드 → 코어 [3] 전용, 계산 스레드 22개 → 코어 [0, 1, 2] | lgpio 신규 스레드 2개, 그중 RT 2개
```

- **신규 스레드 2개가 정상.** lgpio는 핸들 하나당 펄스 생성 + 알림 스레드를 만든다.
  둘 다 계산을 안 하고 자므로 RT여도 안전하다.
- **0개로 나오면 상속이 안 먹은 것** — lgpio가 스레드를 `gpiochip_open()` 때가 아니라
  첫 `tx_pwm()` 때 만든다는 뜻이고, 그러면 워커 스레드 쪽에 걸어야 한다.

실행 중 확인 (`-e`를 붙이면 시스템 전체가 나오니 주의):

```bash
ps -eLo pid,tid,cls,rtprio,psr,comm | awk 'NR==1 || $3!="TS"'   # RT 스레드만
ps -L -o psr= -p $(pgrep -f main.py) | sort | uniq -c  # 코어 분포
```

**FF가 정확히 2개**여야 한다. 5개 이상이면 ③ 복구가 실패한 것이다(워커·저장까지 RT).

### ⚠ 절대 하지 말 것

추적 스크립트 **전체**를 `chrt -f`로 실행하지 말 것. TFLite의 CPU 바운드 스레드가 RT로
돌면 커널 작업까지 밀어내 Pi가 멈출 수 있다. RT는 **계산을 하지 않는 펄스 스레드에만**
줘야 한다 — 위 ③이 그것을 보장한다. 실험에서 `chrt`를 쓴 것은 스레드가 적고 CPU를 안
먹는 `tools/drive_motor.py`/`tools/measure_jitter.py` 단독이었기 때문에 안전했다.

## 채택하지 않은 대안 (되살릴 필요가 생기면)

| 방향 | 왜 안 썼나 |
|---|---|
| lgpio 스레드를 `/proc/self/task` 차집합으로 특정해 승격 | 상속 트릭이 더 안전하고 짧다. 차집합은 창(window)에 다른 스레드가 끼면 **계산 스레드를 RT로 올려버린다** |
| 모터 제어 별도 프로세스 + RT | 작업량 큼(IPC·장부 소유권). 필요해지면 **`rgpiod` 데몬 + `rgpio` 클라이언트**가 지름길 — API가 거의 같고 데몬만 `chrt`로 띄우면 된다 |
| `--threads` 낮추기 | 근본 아님. 추론 FPS만 잃는다 |
| `f_max` 낮추기 | 속도를 잃는다. 같은 효과를 속도 손실 없이 얻으려면 **마이크로스텝 1/16→1/8** (같은 각속도에 펄스 주파수 절반). TODO의 틸트 속도 항목과 함께 볼 것 |
| 패치된 lgpio 재설치 | 절차 유실 + 효과가 검증된 적 없음 + venv마다 재적용. 폐기 |

## 남은 것

- **BLE·릴레이가 아직 안 붙었다.** 붙으면 부하가 늘어 재발할 수 있다. 위 `--timing`
  숫자가 그때의 비교 기준이다.
- **`f_max` 6000 → 10000 복원 검토**: 2026-07-16 하향의 전제(탈조)가 사라졌는지
  부하 조건에서 재실측할 것. 판정 기준은 **언더런 0 유지**. 틸트 속도 부족 문제가 같이
  풀릴 수 있다.
- 그래도 부족하면 부팅 옵션 `isolcpus=3 irqaffinity=0-2` (코드 0줄, 재부팅 필요).
  어피니티는 우리 프로세스의 스레드만 통제하고 커널 스레드·IRQ는 못 옮긴다.

---

## 2. EINVAL 무한 스핀 → 펄스 영구 정지 (2026-08-26 해결)

**증상**: 두 축이 **함께** 멎는다. 프로세스를 껐다 켜야 돌아온다.

**원인**: liblgpio 송출 스레드(`lgPthTx.c`)가 큐 소진 시 `pthTxDelayMicros`를 음수로
계산 → `timespec.tv_nsec` 음수 → `clock_nanosleep`이 EINVAL을 즉시 반환 →
`while(clock_nanosleep(...));`가 같은 인자로 **무한 재시도**. 전 핀의 펄스가 멎는다.

**확정 근거**: 정지 상태에서 `top -H`에 `PR=-11`(SCHED_FIFO 10) 스레드가 `R` 99.9%.
그 우선순위를 받는 건 위 1번 대책이 승격한 lgpio 스레드 2개뿐이고, 그 스레드는
자고·깨서·핀을 토글할 뿐 계산을 하지 않으므로 99.9%는 무한 루프 말고 설명이 없다.
**1번을 먼저 고쳐 둔 덕에 이 지문이 생겼다** — 그전이었다면 평범한 `PR=20` 스레드라
TFLite 워커들과 구분이 안 됐다.

**대책**: `tools/patch_lgpio.sh` — 딜레이 음수 클램프 + sleep 루프를 EINTR만
재시도로 변경. 시스템 파일은 건드리지 않고 `/usr/local/lib`에 패치본을 깔아 로드
우선순위로 덮는다.

> ⚠ **1차 설치는 무효였다.** trixie의 `ld.so` 검색 순서 때문에 구본
> (`/usr/lib/aarch64-linux-gnu/liblgpio.so.1`)이 이겼다.
> `/usr/local/lib/aarch64-linux-gnu/`에도 설치하도록 고친 뒤에야 적용됐다.
> **"깔았으니 됐겠지"가 통하지 않는다** — 아래로 반드시 확인할 것.

```bash
python3 -c "import lgpio,os;[print(l.split()[-1]) for l in open(f'/proc/{os.getpid()}/maps') if 'liblgpio' in l][:1]" \
  | xargs strings | grep -c lgpio-einval-clamp     # 0 이면 미적용
```

⚠ 확인의 범위는 **스핀 소멸**까지다. "두 축 동시 정지가 그 뒤로 재현되지 않았다"를
장시간 검증한 기록은 없다 — 이후 관측은 3번(팬만 멎음)으로 넘어갔다. 그 시기에 두
문제가 겹쳐 있었을 가능성은 배제하지 못한다.

---

## 3. 팬만 멎음 — VREF 과다 (2026-09-02 해결)

**증상**: 두 축 동시 구동에서 **팬만** 멎는다. 틸트는 계속 움직이고, 프로세스를 껐다
켜면 팬이 돌아온다.

**소프트웨어가 아니라는 근거**:
- `--timing`이 팬 구간마다 계속 출력됐다 (언더런 0) = 펄스는 정상 송출 중
- 위치 장부도 계속 증가했다 (`pan=-125.64°`까지)
- 그런데 **팬 드라이버 방열판만 차갑다** = 코일에 전류가 아예 안 흐른다
- 무소음 (탈조라면 시끄럽다)
- EN은 두 축 공유 핀이라 소프트웨어로 팬만 통전을 끊을 수 없다. 복구되는 유일한
  계기가 `disable()`→`enable()` **EN 토글**이라는 점이 드라이버 보호 래치를 가리켰다

**실험**: 팬 단독은 정상. 틸트를 통전만 시키고 스텝을 0으로 두면 정상. 두 축이 동시에
스텝을 밟을 때만 죽고, 틸트 `f_max`를 6000→3000으로 낮추면 **더 오래 버틴다**
(용량 의존).

**원인·대책**: 드라이버 VREF가 과했다 (팬 1.1V / 틸트 1.3V). **양축 0.7V로 통일**하니
같은 조건에서 재현되지 않는다. VREF 조정은 절연 드라이버로, 전원 인가 중 커넥터
탈착 금지.

**배제된 가설**: 전원 용량(24V 5A = 120W, 충분) · 전원 레일 교란(벌크 캡을 달지 않고
VREF만 내려서 해결됐다) · lgpio 큐 고착(언더런 0) · 탈조(무소음) · 기구 걸림(정지
직후 단독 구동은 정상) · 카메라/BLE/릴레이(두 축 추적 단독에서도 재현).

**남은 위험**: 팬이 물리적으로 멈춰도 장부는 계속 쌓여 실제 위치와 벌어지고 그 값이
상태 파일에 저장된다. 다음 실행의 `restore_origin()`이 간 적 없는 각도만큼 되돌리려
한다 (당시 -199.74°가 저장됐다). 호밍 수단이 생기기 전까지 남는 위험이다.
