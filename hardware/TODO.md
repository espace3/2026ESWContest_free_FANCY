# hardware/ TODO

motor_controller.py 본구현(논블로킹) 이후 남은 결정·검증 사항.
해결된 항목은 지우고, 코드에 반영된 내용은 해당 파일 docstring으로 옮길 것.

## 설계 결정

- [ ] **호밍 / 위치 기억** (pan·tilt 공통): 리밋 스위치를 달지, 아니면 위치를
      저장(전원 차단 시 파일 기록 등)해 둘지 결정. 스테퍼는 오픈루프라 탈조가
      나면 소프트웨어 위치와 실제 위치가 어긋난다 — 주기적 재호밍 수단이 필요.
      현재 home()은 "호출 시점의 물리 위치 = 0°" 가정 (임시 방편).
      대안: TMC2209 UART 모드의 StallGuard로 센서리스 호밍 가능한지 검토.
- [ ] **회전 금지 구역(HW deadzone)**: 시스템 조립 후 배선/프레임 간섭으로
      회전하면 안 되는 각도 구역이 생긴다 (모터 자체 제약이 아님 — 조립 후
      확정됨). 확정되면 config에 min/max 각도로 추가하고 control 쪽
      clamp_angle()에 물릴 것. motor_controller는 clamp된 값이 들어온다고 가정.
- [ ] **유휴 시 EN disable 정책**: EN 공유(CNC v3 쉴드 구조)는 확정 — 운용 중에는
      켜둔 채 STEP/DIR로만 축별 제어 (config.py "pins" 주석 참고). 다만 enable
      상태에서는 정지 중에도 유지 전류가 흘러 드라이버/모터가 발열하므로, 장시간
      대상 미검출 등 유휴 상황에서 전체 disable할지(해제 시 pan은 직결이라 외력에
      밀릴 수 있음, tilt는 웜기어 자체 잠금이라 유지됨) 추후 결정.
- [ ] **같은 방향 연장 이동 최적화**: 이동 중 새 목표가 같은 방향으로 더 먼
      지점이어도 지금은 감속 후 재가속한다 (단순화). 추적이 뚝뚝 끊겨 보이면
      감속을 생략하고 순항을 이어가는 로직 검토.

## 확인 필요 (실기 검증 — scripts/verify_pantilt.py)

- [ ] **짧은 이동 실측 한계** (`--short`): 데드존 직후 잔이동(pan 2~5° = 9~22스텝)은
      램프 없이 f_start 저속으로만 나간다. 1/5/10/20/50스텝 단발로 몇 스텝까지
      정확히/조용히 움직이는지 실측. 부족하면 개선 후보: 시작 주파수 하향,
      마이크로스텝 상향(분해능↑·최고 속도↓ 트레이드), 데드존 임계값 조정.
- [ ] **DIR ↔ 각도 부호 매핑**: config stepper.*.dir_for_positive 임시값(1) —
      "화면 오른쪽/아래 = +각도" 추적 방향과 실제 회전 방향이 일치하는지 확인
      후 필요 시 뒤집기.
- [ ] **논블로킹/선점 동작** (`--preempt`, `--track-sim`): 이동 중 목표 갈아타기가
      감속 → 방향 전환 → 재가속으로 부드럽게 이어지는지, 20Hz 목표 송신 중
      move_to가 블로킹되지 않는지, 최종 장부·실위치가 일치하는지 확인.
- [ ] **두 모터 동시 구동**: 현재 모터용 선이 1개라 미실시. 선 확보 후 두 워커
      스레드가 tx_pwm 2핀(STEP 27 + STEP 20)을 동시에 쓰는 상황 검증.
- [ ] **틸트 최고 속도 실측** (`--speed`): lgpio tx_pwm 하드리밋 10kHz 확인됨
      (11000 지정 시 실행 안 됨). 1:100 웜기어 기준 출력축 최대 ≈ 22.5°/s →
      응답 시간 목표 0.5s 내 최대 ~11° 이동. 추적에 부족하면 마이크로스텝
      축소(8→2 등, 스텝당 각도 커짐 · 소음 <35dB 목표와 트레이드오프) 또는
      감속비 변경 검토.
- [ ] **웜기어 백래시**: 틸트 방향 반전 시 유격이 위치 정확도 목표(<2°)에
      영향을 주는지 실측, 필요하면 반전 시 보정 스텝 추가.
